from django.urls import path
from . import views

# urlpatterns = [
#     path('login', views.LoginAPI.as_view()),  # app login
#     path('key-auth', views.KeyCloakLoginAPI.as_view()),  # Keycloak login and refresh token
#     path('password', views.PasswordAPI.as_view()), # create and change password
#     path('request-verify', views.ResetPasswordAPI.as_view()) # Reset password and verify reset password token
# ]
