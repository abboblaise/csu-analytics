# FreeLLMAPI Console Frontend config
server {
    listen          80;
    server_name     $NGINX_FREELLMAPI_CONSOLE_DOMAIN_NAME;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen          443 ssl;
    server_name     $NGINX_FREELLMAPI_CONSOLE_DOMAIN_NAME;

    ssl_certificate /etc/letsencrypt/live/$NGINX_FREELLMAPI_CONSOLE_DOMAIN_NAME/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/$NGINX_FREELLMAPI_CONSOLE_DOMAIN_NAME/privkey.pem;

    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
    client_max_body_size 50M;

    location / {
        proxy_pass http://$NGINX_UPSTREAM_FREELLMAPI_CONSOLE:5173/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $NGINX_SCHEME;
        proxy_set_header X-Forwarded-Port $NGINX_SERVER_PORT;
        error_log /var/log/freellmapi_console_errors.log;
    }
}
